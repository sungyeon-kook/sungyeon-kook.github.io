# Sungyeon Kook academic website files

Copy each file into the matching path of your al-folio repository.

Before deployment:
1. Add your portrait as `assets/img/prof_pic.jpg`.
2. Either add project images and change each `img:` field, or remove the `img:` line from project files.
3. Replace existing sample files in `_news`, `_projects`, `_bibliography/papers.bib`, `_data/socials.yml`, `_data/cv.yml`, and `_pages/about.md`.
4. Keep your repository's other theme files unchanged.
5. Commit and check the GitHub Actions build.
